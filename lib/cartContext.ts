// lib/cartContext.ts
// Placeholder file to prevent import errors.
// The real cart logic is now in: /context/CartContext.tsx

export function useCart() {
  throw new Error(
    "useCart from lib/cartContext.ts is deprecated. Import from '@/context/CartContext' instead."
  );
}
